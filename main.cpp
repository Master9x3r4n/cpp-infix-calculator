#include <iostream>
#include <string>
#include <stack>
using namespace std;

int main() {
    cout << "Hello World!";

    /* PROGRAM FLOW:
    -> Ask for input
    -> Compute value
    -> Output value
    -> Repeat on loop until "END" is typed or file reading has ended
    */

    return 0;
}